<div class="container">
	<div class="row">
		<div class="col col-sm-10 col-sm-offset-1 col-md-8 col-md-offset-2">
			<article>
				<p><a href="/">&larr; back to home</a></p>
				<hr>
				<header>
					<h1>{{page.title}}</h1>
				</header>
				<div class="post">
					{{#if page.image}}<img src="{{{cloudinaryUrl page.image width=750 height=450 crop='fit' }}}" class="img-responsive">{{/if}}
					{{{page.content}}}
				</div>
			</article>
		</div>
	</div>
</div>

